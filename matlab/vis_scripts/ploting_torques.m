function ploting_torques(a)

size(a.time(3000:end,:))
size(a.signals(1,1).values(:,:,3000:end))

createfigure9(a.time(1:end-3000),...
    squeeze(a.signals(1,1).values(:,:,3001:end)),...
    squeeze(a.signals(1,2).values(:,:,3001:end)),...
    squeeze(a.signals(1,3).values(:,:,3001:end)),...
    squeeze(a.signals(1,4).values(:,:,3001:end)));